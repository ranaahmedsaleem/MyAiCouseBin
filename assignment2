# Get the first, middle and last character of a string.
newstr=str(input("enter a string: "))
print("first character of the string is: ",newstr[0])
print("last character of the string is: ",newstr[-1])
print("middle character of the string is: ",newstr[len(newstr)//2])

#program to count and print occurrences of all characters within a string.
dictionary={}
for i in newstr:
    if i in dictionary:
        dictionary[i]+=1
    else:
        dictionary[i]=1
print("occurrences of all characters within a string: ",dictionary)

#program to reverse a sring.
print("reversed string is: ",newstr[::-1])